import React from "react";
import { getBlur } from "@/lib/getBlur";
import { HomePageWrapper } from "@/components";



const Home = () => {
  return (
    <HomePageWrapper
    // blur={blur} blur2={blur2} blur3={blur3} blur4={blur4} blur5={blur5} blur6={blur6}
    />
  );
};

export default Home;
