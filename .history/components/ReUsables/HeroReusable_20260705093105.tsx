import { StaticImageData } from 'next/image'
import React from 'react'

const HeroReusable = ({image, text, header}: {image: StaticImageData, text}) => {
  return (
    <div>HeroReusable</div>
  )
}

export default HeroReusable