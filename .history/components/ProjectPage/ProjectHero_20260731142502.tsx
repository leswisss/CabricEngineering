import React from 'react'
import { IProject } from "@/utils/type";
import styles from "../../styles/ProjectsPage/"

const ProjectHero = ({ project }: { project: IProject }) => {
  return (
    <div>

    </div>
  )
}

export default ProjectHero